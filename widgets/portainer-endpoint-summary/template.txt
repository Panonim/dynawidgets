{{ $endpointName := .JSON.String "Name" }}
{{ $endpointStatus := .JSON.Int "Status" }}
{{ $online := eq $endpointStatus 1 }}
{{ $snapshots := .JSON.Array "Snapshots" }}

<div style="display: grid; gap: 1rem;">
  <div class="flex items-center gap-12">
    <svg aria-hidden="true" viewBox="0 0 24 24" fill="currentColor" style="width: 2.5rem; height: 2.5rem; flex: 0 0 auto;">
      <path d="M12.4 1.5 4.2 5.4v6.2c0 5.1 3.5 9.8 8.2 10.9 4.7-1.1 8.2-5.8 8.2-10.9V5.4L12.4 1.5Zm0 2.2 6.2 2.9v5c0 4-2.6 7.7-6.2 8.8-3.6-1.1-6.2-4.8-6.2-8.8v-5l6.2-2.9Z"/>
      <path d="M9 8h6v2H9zm0 3h6v2H9zm0 3h4v2H9z"/>
    </svg>
    <div class="grow min-width-0">
      <div class="flex items-center gap-10">
        <span class="size-h4 color-highlight text-truncate">{{ $endpointName }}</span>
        <span
          style="width: 0.55rem; height: 0.55rem; border-radius: 50%; flex: 0 0 auto;
            {{ if $online }}background-color: var(--color-positive);{{ else }}background-color: var(--color-negative);{{ end }}"
          title="{{ if $online }}Online{{ else }}Offline{{ end }}"
        ></span>
      </div>
      <div class="size-h6 color-subdue">{{ if $online }}Connected{{ else }}Unavailable{{ end }}</div>
    </div>
  </div>

  {{ if gt (len $snapshots) 0 }}
  {{ $snapshot := index $snapshots 0 }}
  <div style="display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 0.75rem;">
    <div>
      <div class="size-h6 color-subdue">RUNNING</div>
      <div class="size-h3 color-highlight">{{ $snapshot.Int "RunningContainerCount" }}</div>
    </div>
    <div>
      <div class="size-h6 color-subdue">HEALTHY</div>
      <div class="size-h3 color-highlight">{{ $snapshot.Int "HealthyContainerCount" }}</div>
    </div>
    <div>
      <div class="size-h6 color-subdue">STOPPED</div>
      <div class="size-h3 color-highlight">{{ $snapshot.Int "StoppedContainerCount" }}</div>
    </div>
    <div>
      <div class="size-h6 color-subdue">UNHEALTHY</div>
      <div class="size-h3 {{ if gt ($snapshot.Int "UnhealthyContainerCount") 0 }}color-negative{{ else }}color-highlight{{ end }}">
        {{ $snapshot.Int "UnhealthyContainerCount" }}
      </div>
    </div>
    <div>
      <div class="size-h6 color-subdue">VOLUMES</div>
      <div class="size-h3 color-highlight">{{ $snapshot.Int "VolumeCount" }}</div>
    </div>
    <div>
      <div class="size-h6 color-subdue">IMAGES</div>
      <div class="size-h3 color-highlight">{{ $snapshot.Int "ImageCount" }}</div>
    </div>
  </div>
  {{ else }}
    <p class="color-subdue">No Docker snapshot is available for this environment.</p>
  {{ end }}
</div>

required: |
  url: ${PORTAINER_URL}/api/endpoints/${PORTAINER_ENDPOINT_ID}
